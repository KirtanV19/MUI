import { DataGrid } from '@mui/x-data-grid'
import { Box, } from '@mui/material'

const CustomTable = () => {
    return (
        <Box>
            <DataGrid />
        </Box>
    )
}

export default CustomTable;